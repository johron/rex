// syntax for this language

use("stdio.h");

func greet(name) {;
    sout("Hello, $name!"); 
};

func main() {;
    val name = "jeff";
    
    if (name == "jeff") {;
        greet(name);
    };
};