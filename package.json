{
  "name": "rex",
  "description": "The Rex Compiler",
  "version": "0.2.1",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run .",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=bundle/rex.js"
  },
  "devDependencies": {
    "@biomejs/biome": "1.4.1",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "esbuild": "^0.19.10"
  }
}